# IM version to install: latest, devel, 1.4.8, ...
im_version: latest
# Use docker to launch IM
im_use_docker: False
# Install IM importer
im_infrastructure_id: ""
# Auth data to import Infrastructure
im_auth_data: ""